# Сборка для native-sim

#west build -p always -S socketcan-native-sim -b native_sim -- -DCONFIG_GPIO=y -DCONFIG_GPIO_EMUL=y -DCONFIG_HAS_CANBUS=y -DDTC_OVERLAY_FILE=./boards/vend/native_sim/native_sim.overlay

west build -p always -b native_sim -- -DDTC_OVERLAY_FILE=./boards/vend/native_sim/native_sim.overlay -DOVERLAY_CONFIG=./boards/vend/native_sim/native_sim.conf
./build/zephyr/zephyr.exe --can-if=vcan0

Для работы BT 
sudo hciconfig hci0 down
sudo ./build/zephyr/zephyr.exe --bt-dev=hci0

Для работы CAN 
sudo ip link property add dev vcan0 altname zcan0

# Сборка для ESP32C3
west build -p always -b esp32c3_can -- -DBOARD_ROOT=./
west flash
west espressif  monitor
